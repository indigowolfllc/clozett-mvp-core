name: Debug Slack Secret

on: [push]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check if secret exists
        run: |
          if [ -z "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            echo "Secret is EMPTY"
            exit 1
          else
            echo "Secret exists"
          fi
